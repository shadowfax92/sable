import Foundation

final class AppCoordinator {
    /// Starts the app coordinator. The skeleton implementation is replaced once services are wired.
    func start() {}

    /// Stops app-owned resources before termination.
    func stop() {}
}
